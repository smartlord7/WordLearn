<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:allowBackup"> <!-- Add tools:replace to avoid conflicts -->

        <activity
            android:name=".MainMenuActivity"
            android:exported="true"> <!-- Set exported to true for the launcher activity -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="false"> <!-- Set exported to false for other activities -->
        </activity>

        <activity
            android:name=".RankedActivity"
            android:exported="false"> <!-- Set exported to false for other activities -->
        </activity>

        <activity
            android:name=".RegisterActivity"
            android:exported="false"> <!-- Set exported to false for other activities -->
        </activity>

        <activity
            android:name=".ScoreboardActivity"
            android:exported="false"> <!-- Set exported to false for other activities -->
        </activity>

        <activity
            android:name=".ChallengeActivity"
            android:exported="false"> <!-- Set exported to false for other activities -->
        </activity>

        <activity
            android:name=".LoggedInActivity"
            android:exported="false"> <!-- Set exported to false for other activities -->
        </activity>

        <activity
            android:name=".TiersActivity"
            android:exported="false"> <!-- Set exported to false for other activities -->
        </activity>

        <activity
            android:name=".ZoomableTileGridView"
            android:exported="false"> <!-- Set exported to false for other activities -->
        </activity>


        <!-- Add more activities as needed -->

    </application>

</manifest>
